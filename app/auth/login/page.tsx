import Login from '@/components/Auth/Login'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <Login />
        </div>
    )
}

export default page